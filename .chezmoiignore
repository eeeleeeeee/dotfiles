{{ if ne .chezmoi.os "linux" }}
.vimrc
.bashrc
.zshrc
.claude/settings.linux.json
{{ end }}

{{ if ne .chezmoi.os "windows" }}
Documents
bin
.claude/settings.windows.json
{{ end }}

# Never deploy repo meta files
README.md
docs/**
# `claude/` 是 statusline 的 Go source（CI 編譯成 ~/.local/bin/statusline）
claude/**
# `.claude-plugin/` 是 Claude Code plugin marketplace 的 manifest，repo 自身要用
.claude-plugin/**
